const lista = ["Maçã", "Banana", "Laranja", "Pão", "Alface"];

lista.forEach((elemento, indice) => { //Foreach: Facilita a Array 
    console.log(indice + ": " + elemento);//No terminal vamos ter a listagem juntamente de topicos por números mas dessa vez estara começando do 0 a forma padrão do indice.
})