const lista = ["Maçã", "Banana", "Laranja", "Pão", "Alface"]; //Lista, novamente.

for(i = 0;i < lista.length; i++){ //Como antes nosso índice vai até o numero que acabar as opções.
console.log((i + 1)+ ": " + lista[i]); //No terminal vamos ter a listagem juntamente de topicos por números começando do 1.
}