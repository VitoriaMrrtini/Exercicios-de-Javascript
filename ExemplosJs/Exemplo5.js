var a = 1; //Um numero.
var b = 2;
var c = "1"; //Uma string.
var d = "2";

//Quando o codigo rodar sera verificado se as credencias são validas.
console.log(a == b);

console.log(a == c); //verifica o variavel no caso ambos são o mesmo numero.

console.log(typeof a == typeof d); //typeof verifica o tipo, então houvera um erro a variavel "a"(Number) não é do mesmo tipo que o "d"(String).