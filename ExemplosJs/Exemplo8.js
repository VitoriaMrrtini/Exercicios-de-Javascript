const lista = ["Maçã", "Banana", "Laranja", "Pão", "Alface"]; //Lista.

var i = 0; //Nossa lista começa do 0.
while(i < lista.length){ //Foi definido que o tamanho da lista vai até onde tiver as opções, serão 5.
    console.log(lista[i]);
    i++;
}